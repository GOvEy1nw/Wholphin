## Rais Stream implementation tasks

- Read `docs/rais-stream/plan/README.md` and the selected `backlog/tasks/APP-*.md` task before changing code.
- Implement one backlog task only, update its status and implementation notes, then stop.
- Follow the task's smallest relevant verification and avoid unrelated cleanup or refactoring.